{
  "name": "social-network-v123",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm start --prefix api",
    "build": "npm run build --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix api\" \"npm start --prefix frontend\""
  },
  "workspaces": [
    "api",
    "frontend"
  ],
  "engines": {
    "node": ">=8.10"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "husky": "^4.3.0",
    "lint-staged": "^10.4.0",
    "prettier": "^2.1.2"
  },
  "license": "MIT"
}
